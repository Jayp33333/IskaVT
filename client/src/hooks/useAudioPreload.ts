import { useEffect } from "react";

/** Ambient music is initialized in useCustomAmbientMusic (custom or default). */
export default function useAudioPreload() {
  useEffect(() => {
    // Reserved for future one-shot SFX preload.
  }, []);
}
