/**
 * Screen position for the Enter / Exit button (screen center).
 */
export const enterButtonPosition = {
  top: "50%",
  left: "50%",
  transform: "translate(-50%, -50%)",
} as const;

/** How long the waving-dot loading shows before teleporting (ms). */
export const enterTransitionDurationMs = 700;
